//! 可选的 Apollo WGPU / WebGPU 后端 — shader、pipeline、GPU 资源。
//!
//! 不是默认核心依赖。CPU 仍是正确性参考实现。

#![deny(missing_docs)]

/// A0 workspace 冻结用的 crate 占位标记。
pub const CRATE_NAME: &str = "apollo-backend-wgpu";
