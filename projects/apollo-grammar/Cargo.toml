[package]
name = "apollo-grammar"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Apollo：Grammar of Graphics 的 PlotSpec 合同"
publish = false

[dependencies]
apollo-types = { workspace = true }
apollo-data = { workspace = true }

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"
